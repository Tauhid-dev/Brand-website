# Framework Memory

This directory makes development of Project Genesis itself resumable. It follows the
same doctrine as generated projects: current state, handoff, decisions, progress,
completion evidence, and exactly one next action are versioned in the repository.
