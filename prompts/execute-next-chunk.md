# Execute Next Chunk

Read `memory/state.json`, then verify its `current_chunk` exists and is ready. Read the
constitution, doctrine, current state, decision summary, risks, and every item under the
chunk's Required Reading. Run validation before editing.

Execute only the chunk's scope. Do not rely on conversation history. Separate facts,
assumptions, proposals, and accepted decisions. Follow the declared dependencies and
quality gates. Run the stated tests plus proportionate negative, security, failure, and
recovery checks.

Before stopping, record completion evidence, update affected documentation, decisions,
risks, technical debt, status, progress, current state, machine state, session handoff,
and exactly one next action. A blocked chunk remains incomplete and names the authority
or state change needed to resume.
