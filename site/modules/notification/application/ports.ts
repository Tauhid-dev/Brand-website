import type { NotificationChannel, NotificationDelivery, NotificationPreferenceStatus, NotificationTemplate } from "../domain/notification.ts";
export interface NotificationRepository { findTemplate(id: string): Promise<NotificationTemplate | null>; findActiveTemplate(code: string, channel: NotificationChannel): Promise<NotificationTemplate | null>; findPreference(customerId: string, code: string, channel: NotificationChannel): Promise<NotificationPreferenceStatus | null>; findDeliveryByIdempotencyKey(key: string): Promise<NotificationDelivery | null>; findReadyDelivery(at: Date): Promise<NotificationDelivery | null>; enqueue(value: NotificationDelivery): Promise<void>; saveDelivery(value: NotificationDelivery, expectedVersion: number): Promise<void>; setPreference(input: { id: string; customerId: string; code: string; channel: NotificationChannel; status: NotificationPreferenceStatus; updatedBy: string; at: Date }): Promise<void>; publishTemplate(value: NotificationTemplate): Promise<void>; }
export interface NotificationService { request(input: { code: string; channel: NotificationChannel; customerId?: string | null; recipientType: "CUSTOMER" | "ADMIN" | "SYSTEM"; recipientId: string; variables: Readonly<Record<string, unknown>>; idempotencyKey: string; scheduledFor?: Date }): Promise<NotificationDelivery | null>; }
export interface NotificationProvider { send(input: { channel: NotificationChannel; recipientId: string; subject: string | null; body: string; idempotencyKey: string }): Promise<{ providerReference: string }>; }
