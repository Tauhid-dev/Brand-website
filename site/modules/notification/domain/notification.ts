import { DomainValidationError } from "../../shared/domain/errors.ts";
import { EntityId, StableCode, optionalText, requireText } from "../../shared/domain/value-objects.ts";
export type NotificationChannel = "EMAIL" | "SMS" | "IN_APP"; export type NotificationPreferenceStatus = "OPTED_IN" | "OPTED_OUT"; export type NotificationDeliveryStatus = "PENDING" | "PROCESSING" | "SENT" | "FAILED" | "CANCELLED";
const SENSITIVE_KEY = /(password|secret|token|credential|authorization|cookie|api.?key|hash)/i;
export type NotificationTemplateProps = { id: EntityId; code: StableCode; channel: NotificationChannel; version: number; subjectTemplate: string | null; bodyTemplate: string; requiredServiceNotice: boolean; active: boolean; createdAt: Date; updatedAt: Date };
export class NotificationTemplate { readonly props: Readonly<NotificationTemplateProps>; constructor(input: NotificationTemplateProps) { if (!["EMAIL", "SMS", "IN_APP"].includes(input.channel)) throw new DomainValidationError("INVALID_NOTIFICATION_CHANNEL", "Notification channel is invalid."); if (input.channel === "EMAIL" && !input.subjectTemplate) throw new DomainValidationError("NOTIFICATION_SUBJECT_REQUIRED", "Email templates require a subject."); this.props = Object.freeze({ ...input, subjectTemplate: optionalText(input.subjectTemplate, "subjectTemplate", 500), bodyTemplate: requireText(input.bodyTemplate, "bodyTemplate", 20_000) }); } }
export type NotificationDeliveryProps = { id: EntityId; templateId: EntityId; customerId: EntityId | null; recipientType: "CUSTOMER" | "ADMIN" | "SYSTEM"; recipientId: string; channel: NotificationChannel; status: NotificationDeliveryStatus; templateVariables: Readonly<Record<string, unknown>>; idempotencyKey: string; scheduledFor: Date; attemptCount: number; maxAttempts: number; nextAttemptAt: Date | null; providerReference: string | null; errorCategory: string | null; sentAt: Date | null; version: number; createdAt: Date; updatedAt: Date };
export class NotificationDelivery { readonly props: Readonly<NotificationDeliveryProps>; constructor(input: NotificationDeliveryProps) { for (const key of Object.keys(input.templateVariables)) if (SENSITIVE_KEY.test(key)) throw new DomainValidationError("SENSITIVE_NOTIFICATION_VARIABLE", "Notification variables cannot contain credentials or secrets."); if (!Number.isSafeInteger(input.attemptCount) || input.attemptCount < 0 || input.attemptCount > input.maxAttempts || input.maxAttempts < 1) throw new DomainValidationError("INVALID_NOTIFICATION_ATTEMPTS", "Notification attempts are invalid."); this.props = Object.freeze({ ...input, recipientId: requireText(input.recipientId, "recipientId", 255), idempotencyKey: requireText(input.idempotencyKey, "idempotencyKey", 255), templateVariables: Object.freeze({ ...input.templateVariables }), providerReference: optionalText(input.providerReference, "providerReference", 255), errorCategory: optionalText(input.errorCategory, "errorCategory", 120) }); } start(at: Date) { return new NotificationDelivery({ ...this.props, status: "PROCESSING", attemptCount: this.props.attemptCount + 1, nextAttemptAt: null, version: this.props.version + 1, updatedAt: at }); } sent(providerReference: string, at: Date) { return new NotificationDelivery({ ...this.props, status: "SENT", providerReference, errorCategory: null, sentAt: at, version: this.props.version + 1, updatedAt: at }); } failed(errorCategory: string, retryAt: Date | null, at: Date) { const terminal = !retryAt || this.props.attemptCount >= this.props.maxAttempts; return new NotificationDelivery({ ...this.props, status: terminal ? "FAILED" : "PENDING", errorCategory: terminal ? errorCategory : null, nextAttemptAt: terminal ? null : retryAt, version: this.props.version + 1, updatedAt: at }); } }
