# v2 Compatibility Namespace

Status: **Reserved; not yet released**.

v2 will receive the first incompatible public-contract evolution. Its release must
include versioned schemas, a v1→v2 migrator, dry-run and rollback support, fixture
coverage, and a compatibility window. Until release, v2 is not a supported generation
target and must not be represented as production-ready.
