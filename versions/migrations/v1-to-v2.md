# v1 to v2 Migration

Status: specification placeholder until v2 is released.

A v2 release must enumerate every schema, CLI, layout, engine, validator, template, and
state transition change; provide an executable idempotent migrator; preserve unknown
extensions; update cross-references and artifact hashes; and prove rollback or
forward-repair. Do not attempt this migration before the v2 contract is released.
